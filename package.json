{
  "name": "supply-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Shared supply checkout sheets with barcode and receipt scanning, published as a Claude artifact.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint . && node scripts/validate-html.mjs",
    "test": "playwright test",
    "check": "npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "eslint": "^10.11.0",
    "eslint-plugin-html": "^8.2.1",
    "globals": "^17.12.0",
    "html-validate": "^11.16.0"
  }
}
